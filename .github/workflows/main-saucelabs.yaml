name: WebdriverIO via Sauce Labs

on: pull_request

jobs: 
  wdio-sauce-tests:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      
    steps:
      
      - name: Checkout do projeto
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4 
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm install
        
      - name: Set Sauce Labs Credentials
        run: |
          echo "SAUCE_USERNAME=oauth-rossegattites-15-15db7" >> $GITHUB_ENV
          echo "SAUCE_ACCESS_KEY=e14e9f20-b9fc-47b8-9f2c-d100d1aeb73f" >> $GITHUB_ENV
        env:
          SAUCE_USERNAME: oauth-rossegattites-15-15db7
          SAUCE_ACCESS_KEY: e14e9f20-b9fc-47b8-9f2c-d100d1aeb73f
          
      # PASSO CRÍTICO FINAL: Debug Rigoroso e Cópia Forçada
      - name: Prepare config.yml
        run: |
          CONFIG_PATH="v1/exemplos/.sauce/config.yml"
          
          # 1. Verifica se o arquivo existe na origem (AQUI OCORRE O ERRO SE O PATH ESTIVER ERRADO)
          if [ ! -f "$CONFIG_PATH" ]; then
            echo "::error::ERRO FATAL: O arquivo config.yml NÃO FOI ENCONTRADO NO CAMINHO $CONFIG_PATH. Verifique a estrutura de pastas!"
            exit 1
          fi
          
          # 2. Move o arquivo para a raiz do runner (resolve o problema de leitura do Saucectl)
          mv "$CONFIG_PATH" ./config.yml
          
          # 3. DEBUG: Exibe o conteúdo do arquivo
          echo "Conteúdo FINAL do config.yml na raiz do Runner:"
          cat ./config.yml
          
          ls -l config.yml # Confirma que o arquivo está lá
          
      - name: Run tests with saucectl
        uses: saucelabs/saucectl-run-action@v4
        with:
          pass-threshold: 1
          config-file: config.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload test reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: wdio-reports
          path: ./.sauce/reports/**